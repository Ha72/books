<% content_for(:html_title) { @genre.genre } %>

<h3 class="title is-3"><%= @genre.genre %></h3>

<div class="columns is-multiline is-centered is-variable is-8">
    <% @genre.books.each do |book| %>
        <%= render partial: 'books/book', object: book %>
    <% end %>
</div>

<p class="mt-6 is-size-5"><%= link_to "🥨 All Genres", genres_path %></p>
